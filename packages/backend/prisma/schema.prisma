generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}

model AdminStoreEntry {
  key       String   @id
  value     Json
  updatedAt DateTime @updatedAt

  @@map("admin_store_entries")
}
