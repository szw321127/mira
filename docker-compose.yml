services:
  # PostgreSQL Database
  postgres:
    image: ${REGISTRY:-registry.cn-hangzhou.aliyuncs.com}/${IMAGE_OWNER:-szw321127}/rednote_postgres:16-alpine
    pull_policy: missing
    container_name: rednote-postgres
    restart: unless-stopped
    environment:
      - POSTGRES_DB=rednote
      - POSTGRES_USER=rednote
      - POSTGRES_PASSWORD=${DB_PASSWORD:?DB_PASSWORD is required}
      - TZ=Asia/Shanghai
    volumes:
      - postgres-data:/var/lib/postgresql/data
    networks:
      - rednote-network
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U rednote"]
      interval: 10s
      timeout: 5s
      retries: 5

  # Redis Cache
  redis:
    image: ${REGISTRY:-registry.cn-hangzhou.aliyuncs.com}/${IMAGE_OWNER:-szw321127}/rednote_redis:7-alpine
    pull_policy: missing
    container_name: rednote-redis
    restart: unless-stopped
    command: ["redis-server", "--appendonly", "yes"]
    volumes:
      - redis-data:/data
    networks:
      - rednote-network
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5

  # Backend Service
  backend:
    # build:
    #   context: .
    #   dockerfile: packages/backend/Dockerfile
    # 或者使用预构建镜像（取消下面一行的注释，并注释掉上面的 build 部分）
    image: ${REGISTRY:-registry.cn-hangzhou.aliyuncs.com}/${IMAGE_OWNER:-szw321127}/rednote_backend:${IMAGE_TAG:-latest}
    container_name: rednote-backend
    restart: unless-stopped
    command: ["sh", "-c", "node node_modules/prisma/build/index.js migrate deploy && node dist/main.js"]
    # Port 3000 is only accessible internally via docker network, not exposed to host
    expose:
      - "3000"
    environment:
      - NODE_ENV=production
      - CI=true
      - PNPM_CONFIG_CONFIRM_MODULES_PURGE=false
      - PORT=3000
      - TZ=Asia/Shanghai
      - DATABASE_TYPE=postgres
      - DATABASE_HOST=postgres
      - DATABASE_PORT=5432
      - DATABASE_NAME=rednote
      - DATABASE_USER=rednote
      - DATABASE_PASSWORD=${DB_PASSWORD:?DB_PASSWORD is required}
      - REDIS_URL=redis://redis:6379
      - SESSION_SECRET=${SESSION_SECRET:?SESSION_SECRET is required and must be strong}
      - FRONTEND_ORIGIN=https://${APP_DOMAIN:?APP_DOMAIN is required}
      - AI_BASE_URL_ALLOWLIST=${AI_BASE_URL_ALLOWLIST:-}
      - CORS_ORIGINS=https://${APP_DOMAIN},http://frontend
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    networks:
      - rednote-network
    healthcheck:
      test: ["CMD", "node", "-e", "require('http').get('http://localhost:3000/health', (r) => {process.exit(r.statusCode === 200 ? 0 : 1)})"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s

  # Image Worker Service
  worker:
    image: ${REGISTRY:-registry.cn-hangzhou.aliyuncs.com}/${IMAGE_OWNER:-szw321127}/rednote_backend:${IMAGE_TAG:-latest}
    container_name: rednote-worker
    restart: unless-stopped
    command: ["node", "dist/image-worker-runner.js"]
    environment:
      - NODE_ENV=production
      - CI=true
      - PNPM_CONFIG_CONFIRM_MODULES_PURGE=false
      - TZ=Asia/Shanghai
      - DATABASE_TYPE=postgres
      - DATABASE_HOST=postgres
      - DATABASE_PORT=5432
      - DATABASE_NAME=rednote
      - DATABASE_USER=rednote
      - DATABASE_PASSWORD=${DB_PASSWORD:?DB_PASSWORD is required}
      - REDIS_URL=redis://redis:6379
      - SESSION_SECRET=${SESSION_SECRET:?SESSION_SECRET is required and must be strong}
      - FRONTEND_ORIGIN=https://${APP_DOMAIN:?APP_DOMAIN is required}
      - AI_BASE_URL_ALLOWLIST=${AI_BASE_URL_ALLOWLIST:-}
      - IMAGE_WORKER_IDLE_MS=${IMAGE_WORKER_IDLE_MS:-1000}
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
      backend:
        condition: service_healthy
    networks:
      - rednote-network

  # Frontend Service
  frontend:
    # build:
    #   context: .
    #   dockerfile: packages/web-frontend/Dockerfile
    # 或者使用预构建镜像（取消下面一行的注释，并注释掉上面的 build 部分）
    image: ${REGISTRY:-registry.cn-hangzhou.aliyuncs.com}/${IMAGE_OWNER:-szw321127}/rednote_frontend:${IMAGE_TAG:-latest}
    container_name: rednote-frontend
    restart: unless-stopped
    expose:
      - "3000"
    environment:
      - TZ=Asia/Shanghai
      - CI=true
      - PNPM_CONFIG_CONFIRM_MODULES_PURGE=false
      - BACKEND_AGENT_BASE_URL=http://backend:3000
    depends_on:
      - backend
    networks:
      - rednote-network
    healthcheck:
      test: ["CMD", "node", "-e", "require('http').get('http://localhost:3000/', (r) => {process.exit(r.statusCode < 500 ? 0 : 1)})"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 5s

  # HTTPS Reverse Proxy
  caddy:
    image: ${REGISTRY:-registry.cn-hangzhou.aliyuncs.com}/${IMAGE_OWNER:-szw321127}/rednote_caddy:2-alpine
    pull_policy: missing
    container_name: rednote-caddy
    restart: unless-stopped
    ports:
      - "80:80"
      - "443:443"
    environment:
      - APP_DOMAIN=${APP_DOMAIN:?APP_DOMAIN is required}
      - APP_IP=${APP_IP:-127.0.0.1}
    volumes:
      - ./Caddyfile:/etc/caddy/Caddyfile:ro
      - caddy-data:/data
      - caddy-config:/config
    depends_on:
      - frontend
    networks:
      - rednote-network

networks:
  rednote-network:
    driver: bridge

volumes:
  postgres-data:
  redis-data:
  caddy-data:
  caddy-config:
